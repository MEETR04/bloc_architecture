name: bloc_architecture
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  # Routing
  alert_info: ^0.0.8
  auto_route: 

  # State Management & Dependency Injection
  bloc: ^9.0.0

  cached_network_image: ^3.4.1

  # Crypto, Encryption, & Utilities
  connectivity_plus: 

  # UI Design, Custom Controls & Layout
  country_picker: ^2.0.27
  country_pickers: ^3.0.1

  # Crypto, Encryption, & Utilities
  crypto: 

  # Icons
  cupertino_icons: ^1.0.8

  # Crypto, Encryption, & Utilities
  device_info_plus: 12.1.0

  # Networking & HTTP Client
  dio: ^5.9.0

  # UI Design, Custom Controls & Layout
  dropdown_button2: ^2.3.9

  # Crypto, Encryption, & Utilities
  encrypt: 

  file_picker: ^8.1.4
  # Flutter SDK
  flutter:
    sdk: flutter

  # State Management & Dependency Injection
  flutter_bloc: ^9.0.0
  flutter_dotenv: ^6.0.1

  flutter_image_compress: ^2.3.0

  # Crypto, Encryption, & Utilities
  flutter_localization: 

  # Flutter Localizations SDK
  flutter_localizations:
    sdk: flutter

  # UI Design, Custom Controls & Layout
  flutter_screenutil: ^5.9.3
  flutter_svg: ^2.2.1

  # State Management & Dependency Injection
  get_it: ^8.2.0

  # UI Design, Custom Controls & Layout
  gradient_borders: 

  # Local Storage & Database
  hive: ^2.2.3

  # Networking & HTTP Client
  http: 

  image_picker: ^1.1.2

  # UI Design, Custom Controls & Layout
  interval_time_picker: ^4.0.0+12

  # JSON Serialization
  json_annotation: ^4.9.0
  json_serializable: ^6.7.1 # Keep under dependencies as originally defined

  # Crypto, Encryption, & Utilities
  logger: ^2.7.0

  path: ^1.9.0
  path_provider: ^2.1.5
  permission_handler: ^11.3.1

  # Networking & HTTP Client
  pretty_dio_logger: ^1.4.0
  retrofit: ^4.0.1
  shimmer_animation: ^2.2.1

  # Crypto, Encryption, & Utilities
  skeletonizer: ^2.1.3
  toastification: ^3.2.0
  uuid: 

# Asset Generator Settings
flutter_assets_generator:
  style: legacy

dev_dependencies:
  # Code Generation Tools
  auto_route_generator: 
  build_runner: ^2.4.13

  # Code Quality & Linting
  change_app_package_name: ^1.5.0
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^5.0.0

  # Testing
  flutter_native_splash: ^2.4.7
  flutter_test:
    sdk: flutter

  # Code Generation Tools
  hive_generator: ^2.0.1
  retrofit_generator: ^8.2.1

flutter:
  uses-material-design: true

  # Asset Directories
  assets:
     - assets/
     - .env
